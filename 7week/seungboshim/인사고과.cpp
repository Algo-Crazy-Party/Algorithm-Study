#include <string>
#include <vector>

using namespace std;

int solution(vector<vector<int>> scores) {
    int answer = -1;
    int n = scores.size();
    vector<double> avg(n, 0);
    
    return answer;
}